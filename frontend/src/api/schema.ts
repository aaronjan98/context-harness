/**
 * GENERATED FILE — DO NOT HAND-EDIT.
 *
 * This file is generated from the FastAPI backend's OpenAPI spec.
 * It contains TypeScript type definitions only — no runtime code.
 * It is erased at compile time and has zero presence at runtime.
 *
 * To regenerate:
 *   npm run generate:api
 *   (requires the backend to be running at http://localhost:8000)
 *
 * The `paths` and `components` exports are consumed by src/api/client.ts
 * via openapi-fetch to produce a fully typed HTTP client.
 */

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface paths {}

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface components {
  schemas: Record<string, never>
}
