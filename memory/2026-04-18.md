# Session Log — 2026-04-18

## What changed
- Added the first backend scaffold under `server/`
- Introduced `ConversationStore` as the canonical file-layout owner
- Set the default conversation store location to `~/Repositories/conversations`
- Added `docs/README.md` and `docs/backend-scaffold.md` to explain the new
  backend pieces and why they exist
- Added `project-memory/phase-1-checklist.md` for tactical phase progress
- Switched the metadata parser plan to use a real YAML dependency instead of the
  temporary flat parser
- Added canonical message append/read/export behavior to the store and API
- Added backend unit tests for storage behavior and integration tests for the
  FastAPI endpoints
- Ran the backend test suite successfully with `nix develop -c pytest`

## Next step
- Keep backend work focused on robustness and future integrations while the UI
  is built separately
