# Session Log — 2026-04-17

## What changed
- Reframed the project from a Claude-only read-only display panel into a broader
  `agent-display` product concept
- Defined the core product as a file-first conversation workspace where the
  conversation is the source of truth
- Agreed that the app is the canonical writer of conversation files while
  external agents read exports and send new content back through the app
- Chose per-message Markdown files with YAML frontmatter plus a
  conversation-level metadata file
- Chose a phase roadmap that starts with a usable conversation MVP, then adds
  math-native input, forking, graph view, and multi-agent workflows

## Durable decisions promoted
- Added `MEMORY.md` at the project root as the current-state pointer file
- Added `project-memory/` docs for roadmap, storage model, phase 1 scope, and
  durable decisions
- Replaced the old Claude-specific v1 spec with a product-level spec

## Outdated planning removed
- Claude-specific display-panel spec
- MCP setup note tied to Claude startup wiring
- Old tech-stack and v2 planning notes that assumed the earlier product shape

## Next step
- Turn phase 1 into an implementation plan and decide the initial code scaffold
