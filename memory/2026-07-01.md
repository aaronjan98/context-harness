## Code block rendering polish
- Added `highlight.js` to the frontend so imported fenced code blocks can render syntax highlighting from their Markdown language fence.
- Updated `MessageContent` to render fenced code blocks with a language label, copy button, and highlighted HTML while preserving ordinary inline code rendering.
- Added scoped code-block toolbar and highlight token styling in `frontend/src/theme.css` for light and dark themes.
- Verification:
  - `npm run build` -> passed with the existing Vite chunk-size warning
  - `nix develop -c pytest` -> 26 passed
  - `git diff --check`

- Adjusted syntax highlight keyword/meta colors from blue/cyan to red/rose tones per user preference before committing.

## Context Forge outbound Markdown export
- Added backend routes for the current active-thread Markdown export: inline `GET /api/conversations/{conversation_id}/exports/current.md` and download `GET /api/conversations/{conversation_id}/exports/current.md/download`.
- Added API tests for export preview/download and missing-conversation safety.
- Added frontend helpers plus thread toolbar actions for `Copy export` and `Download export`, using the already-generated `exports/current.md`.
- Verification:
  - `npm run build` -> passed with the existing Vite chunk-size warning
  - `nix develop -c pytest` -> 28 passed
  - `git diff --check`

## Selected-message Markdown export
- Added left-side selection dots for each message in the thread so messages can be selected and deselected directly from the conversation view.
- Added selected-message export actions that appear when at least one message is selected: copy selected, download selected, and clear selection.
- Selected exports serialize messages in active thread order with the same heading and attachment-callout shape as full-thread Markdown exports.
- Verification:
  - `npm run build` -> passed with the existing Vite chunk-size warning
  - `nix develop -c pytest` -> 28 passed
  - `git diff --check`

## Export menu declutter
- Replaced the multiple top-right export controls with a single `Export` toolbar button that opens a compact menu.
- Export selection dots now render only while the export menu is open, so normal thread reading stays uncluttered.
- The export menu contains full-thread copy/download plus selected-message copy/download/clear actions, with selected actions disabled until messages are selected.
- Verification:
  - `npm run build` -> passed with the existing Vite chunk-size warning
  - `nix develop -c pytest` -> 28 passed
  - `git diff --check`
