# Session Log — 2026-04-03

## What was done
- Decided to build claude-display to solve LaTeX readability and copy-paste
  friction in Claude Code CLI
- Evaluated MCP bridge vs curl vs writing to file — chose MCP for seamless,
  automatic pushing without manual triggers
- Evaluated xclip, Open WebUI integration, custom HTTP server approaches
- Chose FastAPI + MCP + MathJax 3 stack (see tech-stack.md for rationale)
- Created project scaffold: CONTEXT.md, specs/v1-display-panel.md, memory/
- Decided on v1 scope: read-only display panel, no chat input, no persistence
- Confirmed user is on NixOS; switched from requirements.txt to Nix flake
- Verified mcp 1.15.0, fastapi, uvicorn, httpx, websockets all in nixpkgs unstable
- Wrote flake.nix following same pattern as school/scientific_computing
- Discussed dev containers for future versions (see v2-plans.md)

## Decisions made
- Port: 5050
- Push trigger: automatic via MCP on every message (user + assistant)
- Conversation state: in-memory only for v1
- No API key in v1 — Claude Code is the sole source of messages
- Environment: Nix flake (not pip/venv) — user is on NixOS

## Next session
- Scaffold server/ and static/ directories
- Implement server/main.py (FastAPI + WebSocket)
- Implement server/mcp_server.py
- Build static/index.html + app.js + style.css
- Configure MCP in ~/.claude/settings.json
- Add push_message instruction to ~/.claude/CLAUDE.md
- Run `nix develop` before starting the server
