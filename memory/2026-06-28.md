# Session Log - 2026-06-28

## What changed
- Re-established the product identity as a local, model-agnostic
  conversation/context harness for long-running AI discussions.
- Clarified the distinction from workflow engines like Archon: Archon owns
  deterministic coding workflows, while `agent-display` owns conversation state
  and context reuse.
- Added a root `README.md` that presents the current product vision, status,
  phase roadmap, and development commands.
- Chose `Context Forge` as the product name with the tagline "Context is
  everything." The GitHub/Forgejo repo name should be `context-harness`.

## Current direction
- Keep Phase 1 focused on aligning the frontend/backend API contract before
  building more UI behavior.
- Treat context lenses as an explicit Phase 4 concept so long conversations can
  be exported selectively instead of always sending the entire thread.
